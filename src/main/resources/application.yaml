server:
  port: 5000

spring:
  application:
    name: donation-management-system

  datasource:
    url: jdbc:mysql://localhost:3306/donation_management
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: karangohel2093@gmail.com
    password: ftaf yyjs zpxd ongk
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
    default-encoding: UTF-8

jwt:
  secret: ITC_DONATION_MANAGEMENT_SYSTEM_SECRET_2026!!!
  expiration: 86400000

server:
  port: 5000

spring:
  application:
    name: donation-management-system

  datasource:
    url: jdbc:mysql://localhost:3306/donation_management
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: mysupersecretkeymysupersecretkey
  expiration: 86400000   # ✅ FIXED KEY NAME
